ALTER TABLE workflow_rules
    ADD COLUMN IF NOT EXISTS include_watch_escalations BOOLEAN NOT NULL DEFAULT TRUE,
    ADD COLUMN IF NOT EXISTS critical_high_severity_threshold INTEGER NOT NULL DEFAULT 2,
    ADD COLUMN IF NOT EXISTS critical_repeated_mismatch_threshold INTEGER NOT NULL DEFAULT 2,
    ADD COLUMN IF NOT EXISTS critical_deal_exception_threshold INTEGER NOT NULL DEFAULT 2,
    ADD COLUMN IF NOT EXISTS critical_pipeline_exposure_threshold NUMERIC(19,2) NOT NULL DEFAULT 100000.00,
    ADD COLUMN IF NOT EXISTS high_total_exception_threshold INTEGER NOT NULL DEFAULT 2,
    ADD COLUMN IF NOT EXISTS high_high_severity_threshold INTEGER NOT NULL DEFAULT 1,
    ADD COLUMN IF NOT EXISTS high_repeated_mismatch_threshold INTEGER NOT NULL DEFAULT 1,
    ADD COLUMN IF NOT EXISTS high_pipeline_exposure_threshold NUMERIC(19,2) NOT NULL DEFAULT 25000.00,
    ADD COLUMN IF NOT EXISTS watch_escalation_sla_days INTEGER NOT NULL DEFAULT 7,
    ADD COLUMN IF NOT EXISTS high_escalation_sla_days INTEGER NOT NULL DEFAULT 5,
    ADD COLUMN IF NOT EXISTS critical_escalation_sla_days INTEGER NOT NULL DEFAULT 2,
    ADD COLUMN IF NOT EXISTS watch_escalation_task_due_days INTEGER NOT NULL DEFAULT 1,
    ADD COLUMN IF NOT EXISTS high_escalation_task_due_days INTEGER NOT NULL DEFAULT 1,
    ADD COLUMN IF NOT EXISTS critical_escalation_task_due_days INTEGER NOT NULL DEFAULT 0,
    ADD COLUMN IF NOT EXISTS watch_escalation_task_priority VARCHAR(20) NOT NULL DEFAULT 'MEDIUM',
    ADD COLUMN IF NOT EXISTS high_escalation_task_priority VARCHAR(20) NOT NULL DEFAULT 'MEDIUM',
    ADD COLUMN IF NOT EXISTS critical_escalation_task_priority VARCHAR(20) NOT NULL DEFAULT 'HIGH';
